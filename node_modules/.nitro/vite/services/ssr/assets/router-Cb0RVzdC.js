import { n as defaultRun, t as buildRun } from "./mock-data-DI5bw04E.js";
import { useEffect } from "react";
import { HeadContent, Link, Outlet, Scripts, createFileRoute, createRootRouteWithContext, createRouter, lazyRouteComponent, useRouter } from "@tanstack/react-router";
import { jsx, jsxs } from "react/jsx-runtime";
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
//#region src/styles.css?url
var styles_default = "/assets/styles-CKmh9Gxh.css";
//#endregion
//#region src/lib/lovable-error-reporting.ts
function reportLovableError(error, context = {}) {
	if (typeof window === "undefined") return;
	window.__lovableEvents?.captureException?.(error, {
		source: "react_error_boundary",
		route: window.location.pathname,
		...context
	}, {
		mechanism: "react_error_boundary",
		handled: false,
		severity: "error"
	});
	const message = error instanceof Response ? `Response ${error.status}${error.url ? ` at ${error.url}` : ""}` : error instanceof Error ? error.message : String(error);
	const stack = error instanceof Error ? error.stack : void 0;
	window.__lovableReportRuntimeError?.({
		message,
		...stack !== void 0 && { stack },
		filename: window.location.pathname
	});
}
//#endregion
//#region src/routes/__root.tsx
function NotFoundComponent() {
	return /* @__PURE__ */ jsx("div", {
		className: "flex min-h-screen items-center justify-center bg-background px-4",
		children: /* @__PURE__ */ jsxs("div", {
			className: "max-w-md text-center",
			children: [
				/* @__PURE__ */ jsx("h1", {
					className: "text-7xl font-bold text-foreground",
					children: "404"
				}),
				/* @__PURE__ */ jsx("h2", {
					className: "mt-4 text-xl font-semibold text-foreground",
					children: "Page not found"
				}),
				/* @__PURE__ */ jsx("p", {
					className: "mt-2 text-sm text-muted-foreground",
					children: "The page you're looking for doesn't exist or has been moved."
				}),
				/* @__PURE__ */ jsx("div", {
					className: "mt-6",
					children: /* @__PURE__ */ jsx(Link, {
						to: "/",
						className: "inline-flex items-center justify-center rounded-md bg-primary px-4 py-2 text-sm font-medium text-primary-foreground transition-colors hover:bg-primary/90",
						children: "Go home"
					})
				})
			]
		})
	});
}
function ErrorComponent({ error, reset }) {
	console.error(error);
	const router = useRouter();
	useEffect(() => {
		reportLovableError(error, { boundary: "tanstack_root_error_component" });
	}, [error]);
	return /* @__PURE__ */ jsx("div", {
		className: "flex min-h-screen items-center justify-center bg-background px-4",
		children: /* @__PURE__ */ jsxs("div", {
			className: "max-w-md text-center",
			children: [
				/* @__PURE__ */ jsx("h1", {
					className: "text-xl font-semibold tracking-tight text-foreground",
					children: "This page didn't load"
				}),
				/* @__PURE__ */ jsx("p", {
					className: "mt-2 text-sm text-muted-foreground",
					children: "Something went wrong on our end. You can try refreshing or head back home."
				}),
				/* @__PURE__ */ jsxs("div", {
					className: "mt-6 flex flex-wrap justify-center gap-2",
					children: [/* @__PURE__ */ jsx("button", {
						onClick: () => {
							router.invalidate();
							reset();
						},
						className: "inline-flex items-center justify-center rounded-md bg-primary px-4 py-2 text-sm font-medium text-primary-foreground transition-colors hover:bg-primary/90",
						children: "Try again"
					}), /* @__PURE__ */ jsx("a", {
						href: "/",
						className: "inline-flex items-center justify-center rounded-md border border-input bg-background px-4 py-2 text-sm font-medium text-foreground transition-colors hover:bg-accent",
						children: "Go home"
					})]
				})
			]
		})
	});
}
var Route$5 = createRootRouteWithContext()({
	head: () => ({
		meta: [
			{ charSet: "utf-8" },
			{
				name: "viewport",
				content: "width=device-width, initial-scale=1"
			},
			{ title: "Lovable App" },
			{
				name: "description",
				content: "Lovable Generated Project"
			},
			{
				name: "author",
				content: "Lovable"
			},
			{
				property: "og:title",
				content: "Lovable App"
			},
			{
				property: "og:description",
				content: "Lovable Generated Project"
			},
			{
				property: "og:type",
				content: "website"
			},
			{
				name: "twitter:card",
				content: "summary_large_image"
			},
			{
				name: "twitter:site",
				content: "@Lovable"
			}
		],
		links: [
			{
				rel: "stylesheet",
				href: styles_default
			},
			{
				rel: "preconnect",
				href: "https://fonts.googleapis.com"
			},
			{
				rel: "preconnect",
				href: "https://fonts.gstatic.com",
				crossOrigin: "anonymous"
			},
			{
				rel: "stylesheet",
				href: "https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600;700&family=IBM+Plex+Mono:wght@400;500&display=swap"
			},
			{
				rel: "icon",
				href: "/favicon.ico",
				type: "image/x-icon"
			}
		]
	}),
	shellComponent: RootShell,
	component: RootComponent,
	notFoundComponent: NotFoundComponent,
	errorComponent: ErrorComponent
});
function RootShell({ children }) {
	return /* @__PURE__ */ jsxs("html", {
		lang: "en",
		children: [/* @__PURE__ */ jsx("head", { children: /* @__PURE__ */ jsx(HeadContent, {}) }), /* @__PURE__ */ jsxs("body", { children: [children, /* @__PURE__ */ jsx(Scripts, {})] })]
	});
}
function RootComponent() {
	const { queryClient } = Route$5.useRouteContext();
	return /* @__PURE__ */ jsx(QueryClientProvider, {
		client: queryClient,
		children: /* @__PURE__ */ jsx(Outlet, {})
	});
}
//#endregion
//#region src/routes/index.tsx
var $$splitComponentImporter = () => import("./routes-F-ZTLf76.js");
var Route$4 = createFileRoute("/")({
	head: () => ({ meta: [
		{ title: "GoalForge — AI Agent Goal Dashboard" },
		{
			name: "description",
			content: "Enter a real-world goal and watch GoalForge agents plan, execute tasks, request approvals and deliver results."
		},
		{
			property: "og:title",
			content: "GoalForge — AI Agent Goal Dashboard"
		},
		{
			property: "og:description",
			content: "Enter a real-world goal and watch GoalForge agents plan, execute tasks, request approvals and deliver results."
		},
		{
			property: "og:type",
			content: "website"
		},
		{
			name: "twitter:card",
			content: "summary_large_image"
		}
	] }),
	component: lazyRouteComponent($$splitComponentImporter, "component")
});
//#endregion
//#region src/lib/run-store.server.ts
var current = defaultRun;
var lastDecision = null;
function getRun() {
	return current;
}
function getDecision() {
	return lastDecision;
}
function createGoal(goal) {
	current = buildRun(goal);
	lastDecision = null;
	return current;
}
function setTaskStatus(id, status) {
	current = {
		...current,
		tasks: current.tasks.map((t) => t.id === id ? {
			...t,
			status
		} : t)
	};
	return current;
}
function decideApproval(id, verdict) {
	lastDecision = verdict;
	current = {
		...current,
		approvals: current.approvals.filter((a) => a.id !== id),
		tasks: current.tasks.map((t) => t.status === "awaiting" ? {
			...t,
			status: verdict === "approved" ? "done" : "queued"
		} : t)
	};
	return current;
}
function agentStatus() {
	const running = current.plan.filter((p) => p.status === "running").length;
	return {
		online: true,
		agents: current.activity.map((a) => a.agent),
		active: Math.max(running, 1)
	};
}
//#endregion
//#region src/routes/api/agent-status.ts
var Route$3 = createFileRoute("/api/agent-status")({ server: { handlers: { GET: () => new Response(JSON.stringify(agentStatus()), { headers: { "content-type": "application/json" } }) } } });
//#endregion
//#region src/routes/api/goals.ts
function json(data, status = 200) {
	return new Response(JSON.stringify(data), {
		status,
		headers: { "content-type": "application/json" }
	});
}
var Route$2 = createFileRoute("/api/goals")({ server: { handlers: {
	GET: () => json({
		run: getRun(),
		decision: getDecision()
	}),
	POST: async ({ request }) => {
		const goal = (await request.json().catch(() => null))?.goal?.trim();
		if (!goal) return json({ error: "goal is required" }, 400);
		return json({
			run: createGoal(goal),
			decision: null
		});
	}
} } });
//#endregion
//#region src/routes/api/approvals.$approvalId.ts
var Route$1 = createFileRoute("/api/approvals/$approvalId")({ server: { handlers: { POST: async ({ request, params }) => {
	const verdict = (await request.json().catch(() => null))?.verdict;
	if (verdict !== "approved" && verdict !== "declined") return new Response(JSON.stringify({ error: "invalid verdict" }), {
		status: 400,
		headers: { "content-type": "application/json" }
	});
	const run = decideApproval(params.approvalId, verdict);
	return new Response(JSON.stringify({
		run,
		decision: getDecision()
	}), { headers: { "content-type": "application/json" } });
} } } });
//#endregion
//#region src/routes/api/tasks.$taskId.ts
var allowed = [
	"done",
	"running",
	"queued",
	"awaiting"
];
var Route = createFileRoute("/api/tasks/$taskId")({ server: { handlers: { PATCH: async ({ request, params }) => {
	const status = (await request.json().catch(() => null))?.status;
	if (!status || !allowed.includes(status)) return new Response(JSON.stringify({ error: "invalid status" }), {
		status: 400,
		headers: { "content-type": "application/json" }
	});
	return new Response(JSON.stringify({
		run: setTaskStatus(params.taskId, status),
		decision: getDecision()
	}), { headers: { "content-type": "application/json" } });
} } } });
//#endregion
//#region src/routeTree.gen.ts
var rootRouteChildren = {
	IndexRoute: Route$4.update({
		id: "/",
		path: "/",
		getParentRoute: () => Route$5
	}),
	ApiAgentStatusRoute: Route$3.update({
		id: "/api/agent-status",
		path: "/api/agent-status",
		getParentRoute: () => Route$5
	}),
	ApiGoalsRoute: Route$2.update({
		id: "/api/goals",
		path: "/api/goals",
		getParentRoute: () => Route$5
	}),
	ApiApprovalsApprovalIdRoute: Route$1.update({
		id: "/api/approvals/$approvalId",
		path: "/api/approvals/$approvalId",
		getParentRoute: () => Route$5
	}),
	ApiTasksTaskIdRoute: Route.update({
		id: "/api/tasks/$taskId",
		path: "/api/tasks/$taskId",
		getParentRoute: () => Route$5
	})
};
var routeTree = Route$5._addFileChildren(rootRouteChildren)._addFileTypes();
//#endregion
//#region src/router.tsx
var getRouter = () => {
	const queryClient = new QueryClient();
	return createRouter({
		routeTree,
		context: { queryClient },
		scrollRestoration: true,
		defaultPreloadStaleTime: 0
	});
};
//#endregion
export { getRouter };
