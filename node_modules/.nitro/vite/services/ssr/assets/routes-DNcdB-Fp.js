import { n as defaultRun } from "./mock-data-DI5bw04E.js";
import { useEffect, useState } from "react";
import { jsx, jsxs } from "react/jsx-runtime";
//#region src/lib/api.ts
var TIMEOUT_MS = 3e4;
var CONFIGURED = {
	"BASE_URL": "/",
	"DEV": false,
	"MODE": "production",
	"PROD": true,
	"SSR": true,
	"TSS_DEV_SERVER": "false",
	"TSS_DEV_SSR_STYLES_BASEPATH": "/",
	"TSS_DEV_SSR_STYLES_ENABLED": "true",
	"TSS_DISABLE_CSRF_MIDDLEWARE_WARNING": "false",
	"TSS_INLINE_CSS_ENABLED": "false",
	"TSS_ROUTER_BASEPATH": "",
	"TSS_SERVER_FN_BASE": "/_serverFn/"
}["VITE_API_BASE_URL"]?.replace(/\/+$/, "") ?? "";
/** True when a FastAPI backend URL is configured; otherwise we use the built-in /api routes. */
var isRemoteBackend = CONFIGURED.length > 0;
var BASE_URL = isRemoteBackend ? CONFIGURED : "/api";
var paths = {
	generate: isRemoteBackend ? "/generate" : "/goals",
	run: isRemoteBackend ? "/run" : "/goals",
	task: (id) => isRemoteBackend ? `/tasks/${encodeURIComponent(id)}` : `/tasks/${encodeURIComponent(id)}`,
	approval: (id) => isRemoteBackend ? `/approvals/${encodeURIComponent(id)}` : `/approvals/${encodeURIComponent(id)}`,
	agentStatus: isRemoteBackend ? "/agent-status" : "/agent-status"
};
var ApiError = class extends Error {
	status;
	constructor(message, status) {
		super(message);
		this.name = "ApiError";
		this.status = status;
	}
};
async function request(path, init) {
	const controller = new AbortController();
	const timer = setTimeout(() => controller.abort(), TIMEOUT_MS);
	try {
		const res = await fetch(`${BASE_URL}${path}`, {
			...init,
			signal: controller.signal,
			headers: {
				"content-type": "application/json",
				...init?.headers ?? {}
			}
		});
		if (!res.ok) {
			const detail = await res.text().catch(() => "");
			throw new ApiError(`Backend returned ${res.status}${detail ? `: ${readDetail(detail)}` : ""}`, res.status);
		}
		const text = await res.text();
		if (!text) return {};
		try {
			return JSON.parse(text);
		} catch {
			throw new ApiError("Backend returned a response that was not valid JSON.");
		}
	} catch (err) {
		if (err instanceof ApiError) throw err;
		if (err instanceof DOMException && err.name === "AbortError") throw new ApiError("Backend did not respond in time.");
		throw new ApiError("Could not reach the backend.");
	} finally {
		clearTimeout(timer);
	}
}
function readDetail(text) {
	try {
		const d = JSON.parse(text).detail;
		if (typeof d === "string") return d.slice(0, 200);
		if (Array.isArray(d)) return d.map((e) => str(rec(e)["msg"])).filter(Boolean).join("; ").slice(0, 200);
	} catch {}
	return text.slice(0, 200);
}
var statuses = [
	"done",
	"running",
	"queued",
	"awaiting"
];
function toStatus(value, fallback = "queued") {
	const v = String(value ?? "").toLowerCase();
	if (statuses.includes(v)) return v;
	if (v === "completed" || v === "complete" || v === "finished" || v === "success") return "done";
	if (v === "in_progress" || v === "in-progress" || v === "active" || v === "started") return "running";
	if (v === "pending" || v === "todo" || v === "not_started") return "queued";
	if (v === "waiting" || v === "awaiting_approval" || v === "blocked") return "awaiting";
	if (typeof value === "boolean") return value ? "done" : "queued";
	return fallback;
}
function str(value, fallback = "") {
	return typeof value === "string" && value.trim() ? value : fallback;
}
function num(value, fallback = 0) {
	const n = Number(value);
	return Number.isFinite(n) ? n : fallback;
}
function rec(value) {
	return value !== null && typeof value === "object" && !Array.isArray(value) ? value : {};
}
function list(value) {
	if (Array.isArray(value)) return value.map((v) => typeof v === "string" ? { title: v } : rec(v));
	return [];
}
function pick(source, ...keys) {
	for (const key of keys) if (source[key] !== void 0 && source[key] !== null) return source[key];
}
function normalizeRun(raw, goal) {
	const data = rec(raw);
	const nested = rec(pick(data, "run", "result_data", "data"));
	const run = Object.keys(nested).length > 0 ? nested : data;
	return {
		goal: str(pick(run, "goal", "objective", "prompt"), goal),
		chips: (Array.isArray(pick(run, "chips", "tags", "labels")) ? pick(run, "chips", "tags", "labels") : []).map((c) => str(c)).filter(Boolean),
		plan: list(pick(run, "plan", "steps", "plan_steps")).map((step, i) => ({
			id: str(pick(step, "id", "step_id"), `p${i + 1}`),
			title: str(pick(step, "title", "name", "step", "description"), `Step ${i + 1}`),
			status: toStatus(pick(step, "status", "state")),
			progress: Math.min(100, Math.max(0, num(pick(step, "progress", "percent"), 0)))
		})),
		activity: list(pick(run, "activity", "events", "activity_events", "logs")).map((item, i) => {
			const tone = str(pick(item, "tone"));
			return {
				id: str(pick(item, "id", "event_id"), `a${i + 1}`),
				agent: str(pick(item, "agent", "agent_name", "source"), "Agent"),
				message: str(pick(item, "message", "event", "text", "description")),
				time: str(pick(item, "time", "timestamp", "created_at"), ""),
				kind: str(pick(item, "kind", "type", "category"), ""),
				tone: tone === "mint" || tone === "amber" ? tone : "brand"
			};
		}),
		tasks: list(pick(run, "tasks", "todos", "actions")).map((task, i) => ({
			id: str(pick(task, "id", "task_id"), `t${i + 1}`),
			title: str(pick(task, "title", "name", "task", "description"), `Task ${i + 1}`),
			status: toStatus(pick(task, "status", "state", "done"))
		})),
		approvals: list(pick(run, "approvals", "approval_requests", "pending_approvals")).map((ap, i) => ({
			id: str(pick(ap, "id", "approval_id"), `ap${i + 1}`),
			title: str(pick(ap, "title", "name", "question")),
			detail: str(pick(ap, "detail", "description", "reason", "message"))
		})),
		result: (() => {
			const r = rec(pick(run, "result", "final_result", "outcome"));
			return {
				label: str(pick(r, "label", "title"), "Projected outcome"),
				headline: str(pick(r, "headline", "outcome", "summary", "value"), "—"),
				note: str(pick(r, "note", "subtitle", "detail")),
				confidence: Math.min(100, Math.max(0, num(pick(r, "confidence", "score"), 0)))
			};
		})()
	};
}
function withFallback(run, goal) {
	return {
		...run,
		goal: run.goal || goal || defaultRun.goal
	};
}
var api = {
	isRemote: isRemoteBackend,
	baseUrl: BASE_URL,
	generate: async (goal) => {
		return withFallback(normalizeRun(await request(paths.generate, {
			method: "POST",
			body: JSON.stringify({
				goal,
				prompt: goal
			})
		}), goal), goal);
	},
	getRun: async () => {
		return withFallback(normalizeRun(await request(paths.run), ""), "");
	},
	setTaskStatus: async (id, status, goal) => {
		const run = normalizeRun(await request(paths.task(id), {
			method: "PATCH",
			body: JSON.stringify({ status })
		}), goal);
		return run.tasks.length ? withFallback(run, goal) : null;
	},
	decideApproval: async (id, verdict, goal) => {
		const run = normalizeRun(await request(paths.approval(id), {
			method: "POST",
			body: JSON.stringify({
				verdict,
				decision: verdict,
				approved: verdict === "approved"
			})
		}), goal);
		return run.tasks.length || run.plan.length ? withFallback(run, goal) : null;
	},
	agentStatus: async () => {
		const payload = rec(await request(paths.agentStatus));
		return {
			active: num(pick(payload, "active", "agents_active"), 0),
			online: payload["online"] !== false
		};
	}
};
//#endregion
//#region src/routes/index.tsx?tsr-split=component
var statusText = {
	done: "text-mint",
	running: "text-brand",
	queued: "text-ink-soft",
	awaiting: "text-brand"
};
var navItems = [
	"Dashboard",
	"Agents",
	"History",
	"Settings"
];
function Dashboard() {
	const [run, setRun] = useState(defaultRun);
	const [draft, setDraft] = useState("");
	const [tasks, setTasks] = useState(defaultRun.tasks);
	const [approvals, setApprovals] = useState(defaultRun.approvals);
	const [decided, setDecided] = useState(null);
	const [activeNav, setActiveNav] = useState("Dashboard");
	const [loading, setLoading] = useState(false);
	const [error, setError] = useState(null);
	const applyRun = (next) => {
		setRun(next);
		setTasks(next.tasks);
		setApprovals(next.approvals);
		setDecided(null);
	};
	useEffect(() => {
		let cancelled = false;
		api.getRun().then((next) => {
			if (!cancelled && (next.plan.length || next.tasks.length)) applyRun(next);
		}).catch(() => void 0);
		return () => {
			cancelled = true;
		};
	}, []);
	const submit = async (e) => {
		e.preventDefault();
		const goal = draft.trim();
		if (!goal || loading) return;
		setDraft("");
		setLoading(true);
		setError(null);
		try {
			applyRun(await api.generate(goal));
		} catch (err) {
			setError(err instanceof Error ? err.message : "Goal generation failed.");
		} finally {
			setLoading(false);
		}
	};
	const decide = async (id, verdict) => {
		const prevApprovals = approvals;
		const prevTasks = tasks;
		setError(null);
		setApprovals((a) => a.filter((x) => x.id !== id));
		setDecided(verdict);
		setTasks((ts) => ts.map((t) => t.status === "awaiting" ? {
			...t,
			status: verdict === "approved" ? "done" : "queued"
		} : t));
		try {
			const next = await api.decideApproval(id, verdict, run.goal);
			if (next) {
				setRun(next);
				setTasks(next.tasks);
				setApprovals(next.approvals);
			}
		} catch (err) {
			setApprovals(prevApprovals);
			setTasks(prevTasks);
			setDecided(null);
			setError(err instanceof Error ? err.message : "Could not send that decision.");
		}
	};
	const toggleTask = async (id) => {
		const target = tasks.find((t) => t.id === id);
		if (!target) return;
		const status = target.status === "done" ? "queued" : "done";
		const prevTasks = tasks;
		setError(null);
		setTasks((ts) => ts.map((t) => t.id === id ? {
			...t,
			status
		} : t));
		try {
			const next = await api.setTaskStatus(id, status, run.goal);
			if (next) setTasks(next.tasks);
		} catch (err) {
			setTasks(prevTasks);
			setError(err instanceof Error ? err.message : "Could not update that task.");
		}
	};
	return /* @__PURE__ */ jsxs("div", {
		className: "relative min-h-screen w-full overflow-hidden bg-gradient-to-br from-mist via-mist-2 to-ice font-display text-ink",
		children: [
			/* @__PURE__ */ jsx("div", { className: "pointer-events-none absolute -top-32 -left-24 size-[420px] rounded-full bg-brand/25 blur-[130px] orb-a" }),
			/* @__PURE__ */ jsx("div", { className: "pointer-events-none absolute top-10 right-0 size-[460px] rounded-full bg-brand-2/25 blur-[130px] orb-b" }),
			/* @__PURE__ */ jsx("div", { className: "pointer-events-none absolute bottom-[-140px] left-1/3 size-[440px] rounded-full bg-mint/20 blur-[130px] orb-c" }),
			/* @__PURE__ */ jsxs("div", {
				className: "relative z-10 mx-auto flex max-w-7xl flex-col gap-6 px-6 py-6",
				children: [/* @__PURE__ */ jsxs("header", {
					className: "panel-glass flex items-center justify-between rounded-2xl px-6 py-4",
					children: [
						/* @__PURE__ */ jsxs("div", {
							className: "flex items-center gap-3",
							children: [/* @__PURE__ */ jsx("div", {
								className: "grid size-10 place-items-center rounded-xl bg-gradient-to-br from-brand to-brand-2 text-base font-semibold text-on-brand shadow-lg shadow-brand/30",
								children: "GF"
							}), /* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("div", {
								className: "text-lg font-semibold leading-none tracking-tight",
								children: "GoalForge"
							}), /* @__PURE__ */ jsx("div", {
								className: "mt-1 font-mono text-[10px] uppercase tracking-[0.2em] text-ink-soft",
								children: "Agent Orchestration"
							})] })]
						}),
						/* @__PURE__ */ jsx("nav", {
							className: "hidden items-center gap-1 rounded-xl border border-line/60 bg-panel/40 p-1 lg:flex",
							children: navItems.map((item) => /* @__PURE__ */ jsx("button", {
								type: "button",
								onClick: () => setActiveNav(item),
								className: activeNav === item ? "rounded-lg bg-panel/80 px-4 py-2 text-sm font-medium shadow-sm" : "rounded-lg px-4 py-2 text-sm text-ink-soft hover:text-ink",
								children: item
							}, item))
						}),
						/* @__PURE__ */ jsxs("div", {
							className: "flex items-center gap-3",
							children: [/* @__PURE__ */ jsxs("span", {
								className: "flex items-center gap-2 rounded-full border border-mint/40 bg-mint/10 px-3 py-1.5 font-mono text-[11px] text-ink",
								children: [/* @__PURE__ */ jsx("span", { className: "size-2 rounded-full bg-mint shadow shadow-mint/50" }), " 4 agents live"]
							}), /* @__PURE__ */ jsx("div", {
								className: "grid size-10 place-items-center rounded-full bg-gradient-to-br from-ink to-brand-2 text-xs font-semibold text-on-brand",
								children: "AR"
							})]
						})
					]
				}), /* @__PURE__ */ jsxs("div", {
					className: "grid grid-cols-1 gap-6 lg:grid-cols-12",
					children: [/* @__PURE__ */ jsxs("div", {
						className: "lg:col-span-8",
						children: [/* @__PURE__ */ jsxs("section", {
							className: "panel-glass rounded-2xl p-6",
							children: [
								/* @__PURE__ */ jsxs("div", {
									className: "flex items-center justify-between",
									children: [/* @__PURE__ */ jsx("h2", {
										className: "text-sm font-semibold uppercase tracking-[0.18em] text-ink-soft",
										children: "Goal Input"
									}), /* @__PURE__ */ jsx("span", {
										className: "rounded-full bg-brand/10 px-3 py-1 font-mono text-[11px] text-brand",
										children: "Ready"
									})]
								}),
								/* @__PURE__ */ jsxs("div", {
									className: "mt-4 rounded-xl border border-line/70 bg-panel/60 p-4",
									children: [/* @__PURE__ */ jsx("p", {
										className: "text-lg font-medium leading-snug",
										children: run.goal
									}), /* @__PURE__ */ jsx("div", {
										className: "mt-4 flex flex-wrap gap-2",
										children: run.chips.map((c) => /* @__PURE__ */ jsx("span", {
											className: "rounded-lg border border-line/70 bg-panel/50 px-3 py-1 font-mono text-[11px] text-ink-soft",
											children: c
										}, c))
									})]
								}),
								/* @__PURE__ */ jsxs("form", {
									className: "mt-4 flex gap-3",
									onSubmit: submit,
									children: [/* @__PURE__ */ jsx("input", {
										value: draft,
										onChange: (e) => setDraft(e.target.value),
										disabled: loading,
										className: "flex-1 rounded-xl border border-line/70 bg-panel/50 px-4 py-3 text-sm text-ink outline-none placeholder:text-ink-soft/70 focus:border-brand/50 disabled:opacity-60",
										placeholder: "Describe a real-world goal…"
									}), /* @__PURE__ */ jsx("button", {
										type: "submit",
										disabled: loading,
										className: "rounded-xl bg-gradient-to-br from-brand to-brand-2 px-5 py-3 text-sm font-semibold text-on-brand shadow-lg shadow-brand/30 disabled:opacity-60",
										children: loading ? "Forging…" : "Forge Plan"
									})]
								}),
								loading && /* @__PURE__ */ jsx("p", {
									className: "mt-3 font-mono text-[11px] text-brand",
									children: "Contacting backend · generating plan…"
								}),
								error && /* @__PURE__ */ jsxs("p", {
									role: "alert",
									className: "mt-3 rounded-lg border border-rose/40 bg-rose/10 px-3 py-2 font-mono text-[11px] text-ink",
									children: ["Backend error — ", error]
								})
							]
						}), /* @__PURE__ */ jsxs("section", {
							className: "panel-glass mt-6 rounded-2xl p-6",
							children: [/* @__PURE__ */ jsxs("div", {
								className: "flex items-center justify-between",
								children: [/* @__PURE__ */ jsx("h2", {
									className: "text-sm font-semibold uppercase tracking-[0.18em] text-ink-soft",
									children: "Plan"
								}), /* @__PURE__ */ jsx("span", {
									className: "font-mono text-[11px] text-ink-soft",
									children: "v3 · updated 2m ago"
								})]
							}), /* @__PURE__ */ jsx("ol", {
								className: "mt-5 space-y-4",
								children: run.plan.map((step, i) => /* @__PURE__ */ jsxs("li", {
									className: "flex gap-4",
									children: [
										/* @__PURE__ */ jsx("span", {
											className: `grid size-8 shrink-0 place-items-center rounded-full font-mono text-xs font-medium ${step.status === "done" ? "bg-mint/15 text-mint" : step.status === "running" ? "bg-brand/15 text-brand" : "bg-ink/8 text-ink-soft"}`,
											children: i + 1
										}),
										/* @__PURE__ */ jsxs("div", {
											className: "flex-1",
											children: [/* @__PURE__ */ jsx("p", {
												className: "text-sm font-medium",
												children: step.title
											}), /* @__PURE__ */ jsx("div", {
												className: "mt-2 h-1.5 overflow-hidden rounded-full bg-ink/10",
												children: /* @__PURE__ */ jsx("div", {
													className: `h-full rounded-full transition-all duration-700 ${step.status === "done" ? "bg-mint" : step.status === "running" ? "bg-gradient-to-r from-brand to-brand-2" : "bg-ink/30"}`,
													style: { width: `${step.progress}%` }
												})
											})]
										}),
										/* @__PURE__ */ jsx("span", {
											className: `self-center font-mono text-[11px] ${statusText[step.status]}`,
											children: step.status
										})
									]
								}, step.id))
							})]
						})]
					}), /* @__PURE__ */ jsxs("div", {
						className: "space-y-6 lg:col-span-4",
						children: [
							/* @__PURE__ */ jsxs("section", {
								className: "panel-glass rounded-2xl p-6",
								children: [/* @__PURE__ */ jsxs("div", {
									className: "flex items-center justify-between",
									children: [/* @__PURE__ */ jsx("h2", {
										className: "text-sm font-semibold uppercase tracking-[0.18em] text-ink-soft",
										children: "Agent Activity"
									}), /* @__PURE__ */ jsx("span", { className: "size-2 rounded-full bg-amber shadow shadow-amber/50" })]
								}), /* @__PURE__ */ jsx("ul", {
									className: "mt-5 space-y-4 font-mono text-[12px]",
									children: run.activity.map((a) => /* @__PURE__ */ jsxs("li", {
										className: "flex gap-3",
										children: [/* @__PURE__ */ jsx("span", { className: `mt-0.5 size-2 shrink-0 rounded-full ${a.tone === "brand" ? "bg-brand" : a.tone === "mint" ? "bg-mint" : "bg-amber"}` }), /* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("p", {
											className: "text-ink",
											children: a.message
										}), /* @__PURE__ */ jsxs("p", {
											className: "text-ink-soft/80",
											children: [
												a.time,
												" · ",
												a.kind
											]
										})] })]
									}, a.id))
								})]
							}),
							/* @__PURE__ */ jsxs("section", {
								className: "panel-glass rounded-2xl p-6",
								children: [/* @__PURE__ */ jsx("h2", {
									className: "text-sm font-semibold uppercase tracking-[0.18em] text-ink-soft",
									children: "Tasks"
								}), /* @__PURE__ */ jsx("ul", {
									className: "mt-4 space-y-3 text-sm",
									children: tasks.map((t) => /* @__PURE__ */ jsx("li", { children: /* @__PURE__ */ jsxs("button", {
										type: "button",
										onClick: () => toggleTask(t.id),
										"aria-pressed": t.status === "done",
										className: "flex w-full items-center gap-3 text-left",
										children: [
											t.status === "done" ? /* @__PURE__ */ jsx("span", {
												className: "grid size-5 place-items-center rounded-md bg-mint/15 text-mint",
												children: "✓"
											}) : /* @__PURE__ */ jsx("span", { className: "size-5 rounded-md border-2 border-brand/60" }),
											/* @__PURE__ */ jsx("span", {
												className: "flex-1",
												children: t.title
											}),
											/* @__PURE__ */ jsx("span", {
												className: `font-mono text-[11px] ${t.status === "done" ? "text-ink-soft" : "text-brand"}`,
												children: t.status
											})
										]
									}) }, t.id))
								})]
							}),
							/* @__PURE__ */ jsxs("section", {
								className: "rounded-2xl border border-amber/40 bg-panel/60 p-6 shadow-[0_18px_50px_-20px_color-mix(in_oklab,var(--color-amber)_40%,transparent)] backdrop-blur-2xl",
								children: [/* @__PURE__ */ jsxs("h2", {
									className: "flex items-center gap-2 text-sm font-semibold uppercase tracking-[0.18em] text-ink-soft",
									children: [/* @__PURE__ */ jsx("span", { className: "size-2 rounded-full bg-amber" }), " Approval Request"]
								}), approvals.length === 0 ? /* @__PURE__ */ jsx("p", {
									className: "mt-3 font-mono text-[11px] text-ink-soft",
									children: decided ? `Last request ${decided}. Nothing else waiting.` : "Nothing waiting on you."
								}) : approvals.map((ap) => /* @__PURE__ */ jsxs("div", { children: [
									/* @__PURE__ */ jsx("p", {
										className: "mt-3 text-sm font-medium",
										children: ap.title
									}),
									/* @__PURE__ */ jsx("p", {
										className: "mt-1 font-mono text-[11px] text-ink-soft",
										children: ap.detail
									}),
									/* @__PURE__ */ jsxs("div", {
										className: "mt-4 flex gap-2",
										children: [/* @__PURE__ */ jsx("button", {
											onClick: () => decide(ap.id, "approved"),
											className: "flex-1 rounded-xl bg-gradient-to-br from-brand to-brand-2 px-4 py-2.5 text-sm font-semibold text-on-brand shadow-lg shadow-brand/30",
											children: "Approve"
										}), /* @__PURE__ */ jsx("button", {
											onClick: () => decide(ap.id, "declined"),
											className: "rounded-xl border border-line/70 bg-panel/50 px-4 py-2.5 text-sm font-medium text-ink-soft",
											children: "Decline"
										})]
									})
								] }, ap.id))]
							}),
							/* @__PURE__ */ jsxs("section", {
								className: "panel-glass rounded-2xl p-6",
								children: [/* @__PURE__ */ jsx("h2", {
									className: "text-sm font-semibold uppercase tracking-[0.18em] text-ink-soft",
									children: "Final Outcome"
								}), /* @__PURE__ */ jsxs("div", {
									className: "mt-4 rounded-xl border border-mint/30 bg-gradient-to-br from-panel/70 to-mint/5 p-5",
									children: [
										/* @__PURE__ */ jsx("p", {
											className: "font-mono text-[11px] uppercase tracking-[0.14em] text-mint",
											children: run.result.label
										}),
										/* @__PURE__ */ jsx("p", {
											className: "mt-2 text-xl font-semibold tracking-tight",
											children: run.result.headline
										}),
										/* @__PURE__ */ jsx("p", {
											className: "mt-2 text-sm text-ink-soft",
											children: run.result.note
										}),
										/* @__PURE__ */ jsxs("div", {
											className: "mt-5 border-t border-line/60 pt-4",
											children: [
												/* @__PURE__ */ jsx("p", {
													className: "font-mono text-[11px] uppercase tracking-[0.14em] text-ink-soft",
													children: "Final Plan"
												}),
												/* @__PURE__ */ jsx("p", {
													className: "mt-2 text-sm text-ink-soft",
													children: run.result.summary
												}),
												/* @__PURE__ */ jsx("ol", {
													className: "mt-4 space-y-3",
													children: (run.result.completedSteps ?? run.plan.map((step) => step.title)).map((step, i) => /* @__PURE__ */ jsxs("li", {
														className: "flex gap-3",
														children: [/* @__PURE__ */ jsx("span", {
															className: "grid size-6 shrink-0 place-items-center rounded-full bg-mint/15 font-mono text-[10px] text-mint",
															children: i + 1
														}), /* @__PURE__ */ jsx("p", {
															className: "text-sm font-medium",
															children: step
														})]
													}, `${step}-${i}`))
												})
											]
										}),
										/* @__PURE__ */ jsx("div", {
											className: "mt-5 border-t border-line/60 pt-4",
											children: /* @__PURE__ */ jsx("p", {
												className: "text-sm font-medium text-mint",
												children: "✓ Goal completed and verified"
											})
										})
									]
								})]
							})
						]
					})]
				})]
			})
		]
	});
}
//#endregion
export { Dashboard as component };
